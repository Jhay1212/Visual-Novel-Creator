if ($request->hasFile('main_sprite')) {
    $file = $request->file('main_sprite');
    
    $filename = time() . '_' . $file->getClientOriginalName();

    $path = 'game/character/sprite/base/';
    $file->move(public_path($path), $filename);

    $character->main_sprite = $path . $filename;
}